const SectionTitle = (plugin) => {
    const {title} = plugin;
    return ( 
        <h1 className="text-white text-[3em] font-Poppins tracking-[0.2em] mt-[2.5px] font-[300] textGlow mb-10 mv-[1vh]">{title}</h1>
     );
}
 
export default SectionTitle;